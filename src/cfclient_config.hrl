% Shared config
-define(CONFIG_TABLE, cfclient_config).
-define(CACHE_TABLE, cfclient_cache).
-define(METRICS_TARGET_CACHE_TABLE, cfclient_metrics_target_cache).
-define(METRICS_CACHE_TABLE, cfclient_metrics_cache).
