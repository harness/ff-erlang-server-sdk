%%%-------------------------------------------------------------------
%%% @doc

%%% @end
-module(cfclient_analytics).

-export([]).

-type analytics_config() :: #{enabled := boolean(), push_interval := integer()}.